import { useGamification } from '../context/GamificationContext';

export default function GamificationStatus() {
  const { points, level, streakDays } = useGamification();
  
  // This is a stub component that will be enhanced later
  // For now, it's just a placeholder to make the app build successfully
  
  return null; // Hidden for now
}
